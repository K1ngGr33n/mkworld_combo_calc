# Steps for calculation
Pre calc steps
1. Create text file with **coin collection** and **ground type change** timestamps

Calc steps
1. **Convert** time values to **milliseconds** *(better to work with)*
2. Calculate **time spent** on each **section** *(ground type / coin count)*
3. Calculate **speed / distance**
    - idk what distance is for but sure

Repeat for every combo
and every section
1. Calculate **speed differences from baseline** 
    - (speed: 100m/s + 0,312% per speed level)
2. Use ^ this and CC to calculate each **time difference**
3. **Add** all sections together

Post calc steps
1. **Sort** all values
2. **Display** them nicely because yes